import React from "react";
import "./Icon.css";
import { Grid, Row, Col } from 'react-bootstrap';

const Icon = props => (
    <Row>
        <Col xs={12} className="icon">
            X
        </Col>
    </Row>
);

export default Icon;